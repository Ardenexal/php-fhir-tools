---
description: Filtering, subsetting, and combining functions.
icon: filter
---

# Filtering & Subsetting

**Filtering / subsetting:** `where()`, `select()`, `first()`, `last()`, `tail()`, `skip()`,
`take()`, `single()`, `distinct()`.

**Combining:** `union()`, `intersect()`, `exclude()`, `combine()`.

<!-- TODO: migrate per-function descriptions, signatures, and examples -->

<!-- MIGRATION SOURCE: src/Component/FHIRPath/README.md (Functions: Filtering, Subsetting, Combining) -->
