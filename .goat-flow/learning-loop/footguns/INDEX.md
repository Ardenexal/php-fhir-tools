---
category: index
bucket: footguns
generated: true
---

# Footguns Index

Generated by `goat-flow index`; do not edit by hand. Each row below maps one active footgun entry to its source file with a grep-friendly `(search: "...")` anchor. Re-run `goat-flow index` after adding, editing, renaming, or resolving entries - `goat-flow stats --check` fails while this file is stale. Live entry counts and freshness come from `goat-flow stats`.

- [Subclass constructors must forward to `parent::__construct()` or inherited promoted properties throw](cda-logical-model-parent-constructor.md) (search: "## Footgun: Subclass constructors must forward to `parent::__construct()` or inherited promoted properties throw") - The CDA logical-model generator (`LogicalModelGenerator::generate`) emits each class with its own…
- [choice variants are matched by `instanceof` in declaration order, so a superclass listed before its subclass steals the match](choice-variant-ordering.md) (search: "## Footgun: choice variants are matched by `instanceof` in declaration order, so a superclass listed before its subclass steals the match") - When serializing a `value[x]` choice, `AbstractFHIRNormalizer::resolveChoiceVariant`…
- [building `Models\{version}\...` FQCNs by hand silently defeats profiles](choice-variant-ordering.md) (search: "## Footgun: building `Models\{version}\...` FQCNs by hand silently defeats profiles") - Interpolating a model class name from a version —…
- [a new root-`composer.json` `autoload.psr-4` component entry does not propagate to `demo/composer.json`](demo-app-composer-autoload-drift.md) (search: "## Footgun: a new root-`composer.json` `autoload.psr-4` component entry does not propagate to `demo/composer.json`") - The root `composer.json` and `demo/composer.json` each maintain their own independent…
- [any tool touching `demo/.env` (read, grep, write/append) is denied by a repo policy hook, regardless of content](demo-env-file-blocked-from-agent-access.md) (search: "## Footgun: any tool touching `demo/.env` (read, grep, write/append) is denied by a repo policy hook, regardless of content") - A repo policy hook denies `.env`/secret-file access categorically — not just writes, and not just…
- [Router Table vs Key Resources heading conflict](docs-and-crossrefs.md) (search: "## Footgun: Router Table vs Key Resources heading conflict") - The goat-flow harness checks CLAUDE.md for two distinct things using different check IDs:
- [A restrictive-looking fhirpath context does not restrict when a sibling element context OR-permits](extension-context-or-semantics.md) (search: "## Footgun: A restrictive-looking fhirpath context does not restrict when a sibling element context OR-permits") - `#[FHIRExtensionContext]` attributes use OR semantics: the extension is permitted if ANY declared…
- [an explicit choice-type suffix on a FHIRPath expression returned nothing where the polymorphic name worked](fhirpath-choice-type-suffix-vs-polymorphic-navigation.md) (search: "## Footgun: an explicit choice-type suffix on a FHIRPath expression returned nothing where the polymorphic name worked") - Given a real `Observation` resource fetched live from `hapi.fhir.org` with a JSON field literally…
- [`FHIRPathEvaluator::evaluate()` mutates the passed context, so `getRootResource()` is not stable across calls on a shared context](fhirpath-evaluation-context-mutation.md) (search: "## Footgun: `FHIRPathEvaluator::evaluate()` mutates the passed context, so `getRootResource()` is not stable across calls on a shared context") - `FHIRPathEvaluator::evaluate()` (`src/Component/FHIRPath/src/Evaluator/FHIRPathEvaluator.php`,…
- [a cardinality-incomplete resource constructs, passes PHPStan, and serializes — but is invalid FHIR](generated-model-nullable-cardinality.md) (search: "## Footgun: a cardinality-incomplete resource constructs, passes PHPStan, and serializes — but is invalid FHIR") - The generated model classes (`src/Component/Models/src/{R4,R4B,R5}/`) make every constructor…
- [FHIR `decimal` precision is lost at `json_decode`, before any model code runs](json-decimal-precision-lost-at-decode.md) (search: "## Footgun: FHIR `decimal` precision is lost at `json_decode`, before any model code runs") - The generated models carry `decimal` as a string, not a float — deliberately, because FHIR requires…
- [a swapped-in test double works for the first request in a test, then silently reverts to the real service for the second](kernelbrowser-reboots-between-requests-drops-container-set.md) (search: "## Footgun: a swapped-in test double works for the first request in a test, then silently reverts to the real service for the second") - `Symfony\Bundle\FrameworkBundle\KernelBrowser` reboots the kernel — and therefore rebuilds the…
- [asserting on `$options['auth_bearer']` or the raw `headers` shape inside a `MockHttpClient` callback fails — Symfony resolves semantic options into real headers first](mockhttpclient-normalizes-options-before-callback.md) (search: "## Footgun: asserting on `$options['auth_bearer']` or the raw `headers` shape inside a `MockHttpClient` callback fails — Symfony resolves semantic options into real headers first") - Symfony's `HttpClientInterface::request()` options include semantic shortcuts like `auth_bearer` (a…
- [getExtensionUrl() throws Error on constructor-bypassed Extension objects](model-object-initialization.md) (search: "## Footgun: getExtensionUrl() throws Error on constructor-bypassed Extension objects") - `FHIRExtensionInterface::getExtensionUrl()` documents "returns null when the url property has not…
- [Extension::$value is uninitialized — read with `isset`, never bare access](model-object-initialization.md) (search: "## Footgun: Extension::$value is uninitialized — read with `isset`, never bare access") - Same root cause as `$url` above, for the value carrier.
- [the R5 element that supersedes the allowed-type extension is empty in every shipped package, so reading the spec-correct source yields zero variants](operation-allowed-type-sources.md) (search: "## Footgun: the R5 element that supersedes the allowed-type extension is empty in every shipped package, so reading the spec-correct source yields zero variants") - An `OperationDefinition` parameter typed `Element` or `*` is polymorphic, and the set of concrete…
- [spec prose describing a newer mechanism is not evidence the shipped packages use it](operation-allowed-type-sources.md) (search: "## Footgun: spec prose describing a newer mechanism is not evidence the shipped packages use it") - The generalisation of the entry above, and the reusable part.
- [PHP attribute self::CONST in anonymous classes causes fatal "Undefined constant" error](php-attribute-self-const-anonymous-class.md) (search: "## Footgun: PHP attribute self::CONST in anonymous classes causes fatal") - Fatal "Undefined constant self::PROFILE_A" (or similar) error at attribute instantiation time when…
- [an abstract TestCase base in a scanned suite dir trips ParaTest's failOnWarning (but not plain phpunit)](phpunit-testsuites.md) (search: "## Footgun: an abstract TestCase base in a scanned suite dir trips ParaTest's failOnWarning (but not plain phpunit)") - `phpunit.dist.xml` sets `failOnWarning="true"`.
- [a service that calls `RequestStack::getSession()` breaks any `KernelTestCase`-only (non-`WebTestCase`) test that resolves it](requeststack-getsession-throws-outside-http-request.md) (search: "## Footgun: a service that calls `RequestStack::getSession()` breaks any `KernelTestCase`-only (non-`WebTestCase`) test that resolves it") - `Symfony\Component\HttpFoundation\RequestStack::getSession()` throws `SessionNotFoundException`…
- [the typed deserializer rejects a scalar landing on a complex-typed element (e.g. a bare-string `Reference`)](sdc-template-extract-typed-boundary.md) (search: "## Footgun: the typed deserializer rejects a scalar landing on a complex-typed element (e.g. a bare-string `Reference`)") - SDC template-based extraction mutates a `contained` template as untyped JSON (clone → substitute…
- [Custom constraint attribute missing TARGET_PROPERTY silently produces no violations](symfony-validator-attribute-targets.md) (search: "## Footgun: Custom constraint attribute missing TARGET_PROPERTY silently produces no violations") - Symfony Validator's `AttributeLoader` reads constraint metadata via…
- [enableAttributeMapping() is mandatory — createValidator() alone reads nothing](symfony-validator-attribute-targets.md) (search: "## Footgun: enableAttributeMapping() is mandatory — createValidator() alone reads nothing") - `Validation::createValidator()` does NOT read PHP attributes.
- [the inline-concept path silently dropped/crashed on symbol & collision codes](valueset-enum-case-naming.md) (search: "## Footgun: the inline-concept path silently dropped/crashed on symbol & collision codes") - `FHIRValueSetGenerator` has two code paths that turn ValueSet concepts into enum cases, and until…
- [a two-client test silently asserts against the wrong visitor's page](webtestcase-assertselector-pins-to-current-client.md) (search: "## Footgun: a two-client test silently asserts against the wrong visitor's page") - `Symfony\Bundle\FrameworkBundle\Test\WebTestAssertionsTrait`'s helpers…
- [`Observation?...&_count=3` inside an `itemPopulationContext` can still populate dozens of repeat rows](xfhirquery-count-does-not-bound-total-results.md) (search: "## Footgun: `Observation?...&_count=3` inside an `itemPopulationContext` can still populate dozens of repeat rows") - `XFhirQueryPopulationDataProvider::resourcesForQuery()` (`src/Component/Sdc/src/`) follows every…
