# Copy this file to .env.local and fill in your values.
# Do not commit .env.local to version control.

APP_ENV=dev

###> symfony/routing ###
DEFAULT_URI=http://localhost
###< symfony/routing ###

# XDebug (set in .env.local if needed)
XDEBUG_MODE=off
XDEBUG_SESSION=
